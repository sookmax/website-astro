---
import { cn } from "@website/shared/utils";
export const MAIN_ID = "main";

type Props = {
  class?: string;
};

const { class: className } = Astro.props;
---

<main id={MAIN_ID} class={cn("border-t border-gray-200", className)}>
  <slot />
</main>
